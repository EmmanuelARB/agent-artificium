[SYSTEM NOTIFICATION — SELF REVISION COMPLETE]

`mind/self.txt` has been atomically revised. You will see the revised content
starting with your next inference — directly in the pinned system prompt, or,
when pinned-mind snapshotting keeps that block stable between context
rebuilds, as a runtime change notice included in that next inference's input
until a later rebuild folds it into the pinned block.

Previous version: {{previous_version_path}}
Current Self: {{self_path}}
Reason: {{reason}}
Source events: {{source_event_ids_or_none}}
Before characters: {{before_characters}}
After characters: {{after_characters}}

Continue according to the revised Self. The stable technical instance ID and
filesystem paths have not changed merely because your chosen name may have.

