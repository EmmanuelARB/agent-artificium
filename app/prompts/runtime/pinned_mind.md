[PINNED SELF — as of the last context rebuild from `mind/self.txt`]

{{self_content}}

[END PINNED SELF]

[PINNED META-MEMORY — as of the last context rebuild from `mind/meta_memory.md`]

{{meta_memory_content}}

[END PINNED META-MEMORY]

Self and meta-memory below reflect disk as of the last context rebuild
(process start, a working-memory offload, `compact_context`, or an Infinite
Attention checkpoint compression) — not stale copies recovered from a prior
working-memory-offload checkpoint. When pinned-mind snapshotting is enabled,
this block stays stable between rebuilds so the provider's prompt cache is not
invalidated by an ordinary edit; any such edit reaches you as a runtime change
notice until the next rebuild folds it in here. The complete meta-memory is
loaded without an artificial harness truncation limit; Guidance Notifications
ask you to reduce it when its recurring context cost becomes excessive.
